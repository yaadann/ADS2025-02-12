# Название

Максимальная разница

# Описание

Максимальная разница

## Задача  
Дан массив целых чисел.  
Нужно найти **максимальную разницу** между двумя элементами массива, где больший элемент стоит **правее** меньшего.  

Формально, для массива \(A\) вычислить:  


max
⁡
max⁡(𝐴[𝑗]−𝐴[𝑖]) где 𝑗>𝑖



Если такой пары нет (массив невозрастающий), вывести `0`.

---

## Примеры  

| Входные данные        | Вывод |
|-----------------------|-------|
| `[2, 3, 10, 6, 4, 8, 1]` | `8` (10 - 2) |
| `[7, 9, 5, 6, 3, 2]`     | `2` (9 - 7)  |
| `[1, 2, 3, 4, 5]`        | `4` (5 - 1)  |
| `[5, 4, 3, 2, 1]`        | `0`          |
| `[10, 20, 5, 25]`        | `20` (25 - 5)|

## Ввод
В первой строке задано число $n$ — количество элементов массива.  
Во второй строке записаны $n$ целых чисел $A[i]$.

## Вывод
Выведите одно число — максимальную разницу между элементами массива, где больший элемент стоит правее меньшего.  
Если такой пары нет, выведите `0`.

# Краткая теория

---
title: Максимальная разница
weight: 2
authors:
- Забелич Павел
created: 2025
---

В задачах на массивы часто нужно сравнивать элементы на разных позициях. Здесь цель — найти такую пару, где больший элемент стоит **правее** меньшего, а их разница максимальна. Это классический пример, где простой перебор проигрывает аккуратному линейному проходу.

## Интуиция и постановка

Формально, для массива $A$ требуется вычислить

$$
\max(A[j] - A[i]) \quad \text{где } j > i.
$$

Если массив **невозрастающий**, то подходящей пары нет — ответ равен $0$.

## Наивное решение

**Идея:** перебрать все пары индексов $(i, j)$, где $j > i$, посчитать $A[j] - A[i]$ и взять максимум.

- **Сложность:** $O(n^2)$.
- **Минус:** на больших входах не масштабируется.

## Линейное решение

**Идея:** поддерживать минимальный элемент слева и на каждом шаге обновлять ответ.

- **Шаги:**
  1. Инициализировать **минимум слева** как $A[0]$, **ответ** как $0$.
  2. Для каждого $j$ от $1$ до $n-1$:
     - Обновить ответ: $ans = \max(ans, A[j] - min\_left)$.
     - Обновить минимум слева: $min\_left = \min(min\_left, A[j])$.
- **Сложность:** $O(n)$ по времени, $O(1)$ по памяти.

## Почему это работает

**Ключевая мысль:** для каждого правого элемента $A[j]$ выгоднее всего вычитать **минимальный** элемент среди всех $A[i]$ слева ($i < j$).  
Поддерживая $min\_left$, мы не упускаем ни одной потенциально лучшей пары, а равные или большие левые значения всегда дают не меньшую разницу.

## Граничные случаи

- **Все элементы равны:** ответ $0$.
- **Невозрастающий массив:** ответ $0$.
- **Отрицательные числа:** алгоритм работает без изменений.
- **Большие значения:** используйте типы с достаточным диапазоном (например, 64-битные целые).

### Бонус: короткое упражнение на понимание

> Придумайте массив из 6–8 чисел, в котором линейный алгоритм обновит ответ **ровно один раз**. Объясните, почему так произошло (какой элемент стал единственным кандидатом на максимум и при каком минимуме слева).

