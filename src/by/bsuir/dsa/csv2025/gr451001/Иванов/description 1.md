# Название

Космическая миссия: сбор титана

# Описание

Космическая миссия: сбор титана

## Условие задачи

Для ремонта орбитальной станции экипаж космического корабля должен собрать титан на нескольких планетах. 
Всего имеется `n` планет, причём i‑я планета содержит `titan[i]` тонн титана.
Чтобы добраться и вернуться с планеты, требуется `travel[i]` часов полёта. 
На выполнение всей миссии экипажу отведено не более `h` часов, включая перелёты и добычу.
После прибытия на планету космонавты могут добывать титан со скоростью `k` тонн в час. 
Если на планете осталось меньше чем `k` тонн, они забирают весь остаток и оставшееся время текущего часа отдыхают.
Экипаж стремится работать с минимально возможной скоростью, но при этом должен успеть собрать весь титан 
и вернуться на базу до истечения `h` часов.

Задача: определить минимальную скорость добычи `k` (тонн в час), при которой миссия будет успешно выполнена.

## Ввод

- В первой строке задано число `n` — количество планет.
- Во второй строке указано целое число `h` — общее время (в часах), которое экипаж имеет для выполнения миссии.
- В третьей строке приведены `n` целых чисел `titan[i]`, где каждое число обозначает массу титана (в тоннах) на i‑й планете.
- В четвёртой строке содержатся `n` целых чисел `travel[i]`, определяющих время перелёта (в часах) до соответствующей планеты.

## Вывод

- Целое число — минимальная скорость добычи `k` (тонн в час), при которой экипаж успеет собрать весь титан и вернуться на базу за `h` часов.

## Примечания

Время миссии `h` учитывает как перелёты, так и добычу титана.
Все входные значения — натуральные числа. Диапазоны:
- `1 ≤ n ≤ 100`
- `n ≤ h ≤ 1000`
- `1 ≤ titan[𝑖] ≤ 1000`
- `1 ≤ travel[𝑖] ≤ 100`

## Пример

### Ввод:

    4 
    15
    3 6 7 11
    1 2 1 3

### Вывод:

    4

# Краткая теория

Бинарный поиск

## Идея
Алгоритм делит отрезок поиска пополам и за `O(log n)` шагов находит элемент или границу свойства.

## Алгоритм
1. Задаём границы `l`, `r`.
2. Находим середину `m = (l+r)/2`.
3. Проверяем условие.
4. Сдвигаем `l` или `r`.
5. Повторяем, пока `l != r`.

## В массивах
- Неотсортированный массив → поиск за `O(n)`.
- Отсортированный массив → бинарный поиск за `O(log n)`.

## Применение
- Поиск элемента.
- Подсчёт количества вхождений.
- Нахождение границы монотонного свойства.

