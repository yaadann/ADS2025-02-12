# Название

Кратчайший путь с ограничением числа переходов

# Описание

Кратчайший путь с ограничением числа переходов.
Дан ориентированный взвешенный граф из n вершин и m рёбер.
Каждое ребро имеет неотрицательный вес.

Также задано число K — максимальное количество рёбер, которое разрешено использовать в пути.

Необходимо найти кратчайший путь от вершины start до вершины end, используя не более K рёбер.
Если подходящего пути не существует — вывести -1.

Формат входных данных

Числа n и m

m строк формата u v w

Строка start end K

Формат вывода
Одно число — длина кратчайшего пути, либо -1, если путь не существует.

# Краткая теория

Поиск кратчайших путей в графах — это задача нахождения минимальной суммарной стоимости маршрута между двумя вершинами. Если веса рёбер неотрицательные, классически используется алгоритм Дейкстры.

Его основная идея — хранить для каждой вершины текущую оценку минимальной стоимости пути:

d[v]=минимально известная стоимость пути от start до v

И на каждом шаге выбирать вершину с минимальным 

d[v] среди необработанных, после чего «улучшать» значения для её соседей.

При релаксации (попытке улучшения пути) используется характерное сравнение:

d[v]=min(d[v], d[u]+w(u→v))

Это ключевой принцип большинства алгоритмов кратчайших путей.

Однако, когда вводится ограничение на максимальное число рёбер 
𝐾
K, обычный алгоритм Дейкстры не подходит: он не отслеживает, сколько шагов использовано.

Чтобы учитывать ограничение, удобнее рассматривать пути, различающиеся числом переходов. В этом случае для каждой вершины можно хранить оценку стоимости пути при фиксированном количестве рёбер:

dist[k][v]=минимальная стоимость пути к вершине v с использованием ровно k рёбер

Тогда стоимость пути на следующем шаге может быть выражена формулой:

dist[k][v]=min(dist[k][v], dist[k−1][u]+w(u→v))

Эта формула отражает идею постепенного «наращивания» пути, добавляя по одному ребру.

Такой подход напоминает пошаговую (по слоям) обработку графа: каждый слой соответствует маршрутам длины 
1,2,...,K.
Ограничение 
K не позволяет использовать слишком длинные цепочки, даже если их стоимость меньше.

